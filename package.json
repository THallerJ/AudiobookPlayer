{
	"dependencies": {
		"axios": "^0.24.0",
		"colorthief": "^2.3.2",
		"connect-mongodb-session": "^3.1.1",
		"cors": "^2.8.5",
		"crypto-js": "^4.1.1",
		"dotenv": "^10.0.0",
		"express": "^4.17.1",
		"express-session": "^1.17.2",
		"express-static-gzip": "^2.1.6",
		"mongoose": "^6.0.10",
		"passport": "^0.5.0",
		"passport-google-oauth2": "^0.2.0",
		"passport-google-oauth20": "^2.0.0"
	},
	"name": "server",
	"version": "1.0.0",
	"main": "index.js",
	"scripts": {
		"start": "node server",
		"dev": "nodemon server",
		"create": "npm install && cd client && npm install && npm run build && cd .. && npm run start",
		"heroku-postbuild": "cd client && npm install && npm install --only=dev --no-shrinkwrap && npm run build",
		"install-deps": "npm install && cd client && npm install",
		"build": "cd client && npm run build",
		"watch": "cd client && npm run watch"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"description": "",
	"devDependencies": {
		"nodemon": "^2.0.13"
	}
}
